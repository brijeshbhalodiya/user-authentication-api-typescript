# User authentication APIs using Typescript

### Project Setup

```
npm install
```

### Run server for development

- Create an empty `.env` file in root folder, copy `.env.example` contents into it, and fill in the details.

```
npm run dev
```
